name: dass
description: A new Flutter project for the DASS app.

publish_to: 'none' # Remove this line if publishing to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <4.0.0"

dependencies:
   # Core Firebase package
  firebase_auth: ^5.3.4
  firebase_database: ^11.2.0 # Realtime Database
  #firebase_storage: ^11.1.0  # (Optional) Firebase Storage for images
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
  firebase_core: ^3.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/image/logo.png
    - assets/image/homebg.jpg
